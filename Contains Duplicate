class Solution {
    public boolean containsDuplicate(int[] nums) {
        HashSet<Integer> find = new HashSet<>();
        for (int num : nums){
            if (find.contains(num))
            return true;

            find.add(num);
        }
        return false;
    }
}


Way to solve--- In this I created a hashset in which I pass all the values pf an aaray if there is any value which is already 
in the hashset then return true otherwise false.

thing i learned---(hashset, add, contains).
